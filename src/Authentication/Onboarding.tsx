import React from 'react';
import { View, Text } from 'react-native';

const Onboarding = () => {
  return (
    <View style={{flex: 1, backgroundColor: '#fac'}}>
      <Text />
    </View>
  );
};

export default Onboarding;